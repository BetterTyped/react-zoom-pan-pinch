import { Meta, Story, ArgsTable, Canvas } from "@storybook/blocks";

import { TransformWrapper } from "../../../components";
import { argsTypes } from "../../types/args.types.ts";
import { Example } from "./example";

export const Template = (args) => <Example {...args} />;

<Meta
  title="Basic/Image"
  component={TransformWrapper}
  argTypes={argsTypes}
/>

# Image

The most common use-case: wrap an **image** in `TransformWrapper` and
`TransformComponent` to make it zoomable and pannable. Use the **tab switcher**
below the viewport to swap between three image sizes and see how the library
handles different content dimensions — from small thumbnails to high-resolution
photos.

Pan by dragging, zoom with the scroll wheel or pinch gesture, and use the
floating controls to zoom in, zoom out, reset, or center the view. The scale
badge in the corner tracks the current zoom level in real time.

<Canvas>
  <Story name="Image">{(args) => <Template {...args} />}</Story>
</Canvas>

## Component API

<ArgsTable story="Image" />
