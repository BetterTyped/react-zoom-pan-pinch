import { Meta, Story, ArgsTable, Canvas } from "@storybook/blocks";

import { TransformWrapper, TransformComponent, MiniMap } from "../../../components";
import { argsTypes } from "../../types/args.types.ts";
import { Template } from "./example";

<Meta
  title="Components/Mini Map"
  component={TransformWrapper}
  argTypes={argsTypes}
/>

# Mini Map

A **miniature overview** that mirrors the main content and highlights the
currently visible viewport. The `MiniMap` component renders a scaled-down
duplicate of the content in a floating panel — useful for large canvases,
documents, or maps where spatial awareness matters.

<Canvas>
  <Story name="Mini Map">{(args) => <Template {...args} />}</Story>
</Canvas>

## Component API

<ArgsTable story="Mini Map" />
