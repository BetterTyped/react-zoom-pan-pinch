import { Meta, Story, ArgsTable, Canvas } from "@storybook/blocks";

import { TransformWrapper, TransformComponent } from "../../../components";
import { argsTypes } from "../../types/args.types";
import { Example } from "./example";

export const Template = (args) => <Example {...args} />;

<Meta
  title="Advanced/Stress Test"
  component={TransformWrapper}
  argTypes={argsTypes}
/>

# Stress Test

A **1,000-image grid** (100 columns) to verify the library handles large DOM
trees without performance degradation. Pan and zoom remain smooth because the
library applies a single CSS `transform` on the container — individual child
elements are never touched during interactions.

<Canvas>
  <Story name="Stress Test">{(args) => <Template {...args} />}</Story>
</Canvas>

## Component API

<ArgsTable story="Stress Test" />
